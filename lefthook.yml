# https://lefthook.dev/configuration/

pre-commit:
  commands:
    format:
      run: pnpm exec biome check --write --staged
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck
